queue_rules:
  - name: default
    merge_conditions:
      - "#approved-reviews-by>=1"
      - "label=go-merge"
pull_request_rules:
  - name: q
    conditions: ["label=ready"]
    actions:
      queue: {name: default}
