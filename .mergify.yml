queue_rules:
  - name: default
    merge_conditions:
      - label=approved
      - label=qa-passed
    merge_method: merge
pull_request_rules:
  - name: q
    conditions: [label=ready]
    actions:
      queue: {name: default}
