pull_request_rules:
  - name: render-sinks
    conditions:
      - "label=go"
    actions:
      comment:
        message: "branch={{ head }} title={{ title }}"
      post_check:
        title: "chk {{ head }}"
        summary: "sum branch={{ head }} title={{ title }}"
